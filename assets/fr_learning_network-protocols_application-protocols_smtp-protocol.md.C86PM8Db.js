import{_ as e,c as o,o as s,a1 as r,aR as t}from"./chunks/framework.CgL1aY5S.js";const P=JSON.parse('{"title":"Le protocole SMTP","description":"","frontmatter":{},"headers":[],"relativePath":"fr/learning/network-protocols/application-protocols/smtp-protocol.md","filePath":"fr/learning/network-protocols/application-protocols/smtp-protocol.md"}'),l={name:"fr/learning/network-protocols/application-protocols/smtp-protocol.md"},i=r('<h1 id="le-protocole-smtp" tabindex="-1">Le <code>protocole SMTP</code> <a class="header-anchor" href="#le-protocole-smtp" aria-label="Permalink to &quot;Le `protocole SMTP`&quot;">​</a></h1><p>Le <code>protocole SMTP</code> (Simple Mail Transfer Protocol) est utilisé pour la <strong>transmission de courriels (e-mails) sur le réseau Internet</strong>. Il définit comment les messages électroniques sont envoyés d&#39;un serveur de messagerie à un autre, ainsi que du client de messagerie à un serveur.</p><h2 id="fonctionnement" tabindex="-1">Fonctionnement <a class="header-anchor" href="#fonctionnement" aria-label="Permalink to &quot;Fonctionnement&quot;">​</a></h2><p>Pour illustrer le fonctionnement du <code>protocole SMTP</code>, prenons l&#39;exemple de l&#39;envoi d&#39;un e-mail depuis l&#39;application Mail d&#39;un Mac vers une adresse Gmail.</p><p><img src="'+t+'" alt="smtp-protocol"></p><ol><li><strong>Émission de la Requête SMTP par le client de messagerie</strong> : L&#39;application Mail sur le Mac utilise le <code>protocole SMTP</code> pour préparer l&#39;e-mail à envoyer. Elle se connecte au serveur SMTP d&#39;Apple via une connexion TCP, généralement sur le port 587.</li><li><strong>Transmission via TCP/IP</strong> : Le message est encapsulé dans des segments TCP/IP pour être <strong>transmis au serveur <code>SMTP</code> d&#39;Apple</strong>.</li><li><strong>Réception par le Serveur <code>SMTP</code> d&#39;Apple (MTA)</strong> : Le serveur de transfert de courrier (MTA - Mail Transfer Agent) d&#39;<strong>Apple reçoit la requête <code>SMTP</code></strong>. Il vérifie les en-têtes et le contenu du message, s&#39;assurant de l&#39;authenticité et de l&#39;intégrité des données.</li><li><strong>Résolution DNS</strong> : Le serveur MTA d&#39;Apple interroge le serveur DNS pour obtenir l&#39;adresse IP du serveur <code>SMTP</code> de Google, correspondant au domaine de l&#39;adresse e-mail de destination (ex: gmail.com).</li><li><strong>Relais <code>SMTP</code> vers le Serveur <code>SMTP</code> de Google</strong> : Utilisant les informations DNS, le serveur MTA d&#39;Apple établit une connexion <code>SMTP</code> avec le serveur <code>SMTP</code> de Google. Le message est transmis via une nouvelle session <code>SMTP/TCP</code>.</li><li><strong>Réception par le Serveur <code>SMTP</code> de Google (MTA)</strong> : Le serveur MTA de Google, également en écoute sur le port 587 ou 465, reçoit la requête <code>SMTP</code>. Il vérifie les en-têtes pour s&#39;assurer que le message peut être relayé vers le destinataire final.</li><li><strong>Relais Interne chez Google</strong> : Le serveur MTA de Google peut passer le message à d&#39;autres serveurs internes pour le rapprocher du serveur de livraison approprié, en utilisant des mécanismes internes de routage et de gestion des e-mails.</li><li><strong>Livraison au Serveur MDA de Google</strong> : <ul><li>Le message atteint finalement un serveur MDA (Mail Delivery Agent) de Google, chargé de la livraison finale du message dans la boîte de réception du destinataire. Le MDA stocke le message dans la boîte aux lettres du destinataire sur le serveur.</li><li>Lorsque le destinataire accède à son compte Gmail, le serveur MDA utilise les <code>protocoles IMAP</code> (Internet Message Access Protocol) ou POP3 (Post Office Protocol) pour synchroniser et délivrer le message au client de messagerie du destinataire.</li></ul></li></ol><div class="info custom-block"><p class="custom-block-title">IMAP</p><p>permet une synchronisation complète des messages entre le serveur et le client, permettant l&#39;accès aux messages depuis plusieurs appareils tout en conservant les e-mails sur le serveur.</p></div><div class="info custom-block"><p class="custom-block-title">POP3</p><p>télécharge les messages sur le client et les supprime généralement du serveur après le téléchargement, ce qui est plus adapté pour un usage unique d&#39;un seul appareil.</p></div><ol start="9"><li><strong>Réception de l&#39;E-mail</strong> : Le destinataire peut maintenant lire l&#39;e-mail sur son client de messagerie, qu&#39;il s&#39;agisse de l&#39;application Gmail sur un navigateur, un client de messagerie comme Outlook, ou une application mobile.</li></ol><h2 id="protocole-smtp-sur-le-web" tabindex="-1">Protocole SMTP sur le Web <a class="header-anchor" href="#protocole-smtp-sur-le-web" aria-label="Permalink to &quot;Protocole SMTP sur le Web&quot;">​</a></h2><p>Si un e-mail est envoyé depuis une interface webmail, comme Gmail via un navigateur web tel que Chrome, le processus suit un chemin légèrement différent :</p><ol><li><p><strong>Envoi via le Navigateur</strong> :</p><ul><li>L&#39;utilisateur rédige un e-mail sur l&#39;interface webmail de Gmail dans Chrome.</li><li>Une requête <code>HTTPS</code> est envoyée au serveur web de Gmail, où elle est traitée et préparée pour l&#39;envoi via <code>SMTP</code>.</li></ul></li><li><p><strong>Transmission au Serveur de Gmail</strong> :</p><ul><li>Le serveur web de Gmail agit comme un proxy et envoie l&#39;e-mail au serveur MTA de Google via le protocole <code>SMTP</code>.</li></ul></li></ol><p>Ce processus utilise le protocole <code>HTTP/HTTPS</code> pour la transmission initiale entre le client (navigateur) et le serveur web de Google, avant de basculer vers <code>SMTP</code> pour la transmission entre serveurs de messagerie. :::</p>',13),a=[i];function n(c,u,d,p,m,g){return s(),o("div",null,a)}const T=e(l,[["render",n]]);export{P as __pageData,T as default};
